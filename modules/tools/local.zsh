# ==============================================================================
# Local Overrides (optional)
# ~/.zshrc.local is loaded if it exists — never tracked in version control
# Place machine-specific config here: work aliases, local paths, secrets
# ==============================================================================
[[ -f ~/.zshrc.local ]] && source ~/.zshrc.local
